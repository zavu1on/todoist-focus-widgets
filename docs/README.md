# Документация приложения Focus Widgets

**Focus Widgets** — мобильное приложение на Android, которое позволяет создавать яркие крупные виджеты со списком задач из **Todoist**, отфильтрованных по проекту, приоритету, дате выполнения или лейблу


## Стадия проекта

Проект находится на стадии инициализации: описано техническое задание (**SPECIFICATION.md**), сгенерирован базовый Expo-скаффолд без экранов и бизнес-логики. Реализация функциональности из **SPECIFICATION.md** ещё не начата


## Файловая структура

```
docs/
├── README.md             — навигационный хаб (этот файл)
├── SPECIFICATION.md      — техническое задание: авторизация по токену Todoist, экран списка виджетов, виды виджетов (Pin, List), хранение конфигурации фильтров, создание/редактирование/удаление виджетов, обновление данных, ссылка на дизайн
├── TODOIST_SYNC_API.md   — ресёрч Todoist Sync API: sync_token, resource_types, commands, доступ через @doist/todoist-sdk, применение для обновления данных виджетов
├── REACT_NATIVE_ANDROID_WIDGET.md — ресёрч react-native-android-widget: архитектура рендера, регистрация провайдера, task handler и события жизненного цикла, обновление без пересоздания, клики, ограничения
├── CODE_STYLE_GUIDE.md   — руководство по архитектуре (FSD) и оформлению кода: технологический стек, naming, роутинг Expo Router, лучшие практики TanStack Query и React Hook Form + Zod, SDLC
├── assets/               — изображения для документации
│   └── preview.png       — превью экрана списка виджетов для корневого README.md
└── decisions/            — ADR (Architecture Decision Records)
    ├── README.md         — паттерн, процесс и шаблон ADR
    ├── 00-use-adr.md     — использование паттерна ADR
    └── 01-single-sqlite-connection.md — инвариант одного SQLite-соединения на JS-рантайм, устраняющий use-after-free нативного хэндла
```


## Правила форматирования

- Документация пишется на русском языке
- Между параграфами любого уровня отступ в две строки
- Все определения выделяются **полужирным**. Определения бизнес-сущностей дополнительно переводятся на английский, как они будут прописываться в коде
- При упоминании бизнес-сущностей в предложении они выделяются **полужирным**, а также пишутся с большой буквы
- Названия полей выделяются <u>подчёркиванием</u>
- Акцент на слове или использование слова в переносном смысле задаётся *курсивом*
- Между элементами списка могут как идти пропуски в строку (для всех элементов), так их может и не быть (также для всех элементов)
- Инструкции даются в виде безличного предложения, например: "необходимо обновить дерево **Файловая структура** в этом же коммите"


## Актуализация навигационного хаба

Этот файл — единственный источник маршрутизации по `docs/`. При добавлении, удалении или изменении сути (не опечатки) любого файла в `docs/` — необходимо обновить дерево **Файловая структура** в этом же коммите. Тот же принцип действует для AI-обвязки: см. раздел «Актуализация» в [`../CLAUDE.md`](../CLAUDE.md).
